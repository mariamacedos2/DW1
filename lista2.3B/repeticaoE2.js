// 02--Faça uma função que dado um número N, imprime na tela todos os valores entre N e 1 (ordem decrescente).

function decrescente(N){
    for(let i = N; i >= 1; i--){
        console.log(i)
    }
}
decrescente(8)